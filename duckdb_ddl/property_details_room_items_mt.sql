CREATE TABLE property_details_room_items_mt (s
  id VARCHAR PRIMARY KEY,
  prop_id VARCHAR,
  room_name VARCHAR,
  dimensions VARCHAR,
  dimensions_alt VARCHAR,
  description VARCHAR,
  ingested_at TIMESTAMP
);